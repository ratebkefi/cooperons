<link  href={{ asset('bundles/Edtapp/css/sidebar_navigation.css')}} rel='stylesheet' type='text/css' />


<div class="topnavbar navbar">
    <a class="logo" href="http://www.cooperons.com" target="_blank" title="cooperons.com"></a>
     {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_OPERATOR') %}
        <ul class="nav ace-nav pull-right">
            <li><a id="tooltip-help"><i class="fa fa-bell icon-animated-bell" title="Notifications"></i><span class="badge badge-important"></span></a></li>
            <li><a id="tooltip-home" href="{{path('dispatcher')}}" title="Accueil"><i class="fa fa-home"></i></a></li>
            <li><a id="tooltip-disconnect" href="{{path('fos_user_security_logout')}}" title="Déconnexion"><i class="fa fa-power-off"></i></a></li>
        </ul>

        <script type="text/javascript">
            $(document).ready(function() {
            });
        </script>

    {% endif %}
    {% if program is defined %}
        {% if isEasy is not defined %}
            {% set isEasy = program.isEasy?1:0 %}
        {% endif %}
        <div class="faq">
            <a href="{{path('faq_corporate', {'isEasy' : isEasy})}}">
                <i class="fa fa-question-circle"></i>
            </a>
        </div>
    {% else %}
        <div class="faq">
            <a href="{{path('faq_member')}}">
                <i class="fa fa-question-circle"></i>
            </a>
        </div>
    {% endif %}
</div>